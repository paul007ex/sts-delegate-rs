apiVersion: v1
kind: Service
metadata:
  name: sts-delegate-rs
  namespace: sts-delegate-rs
  labels:
    app.kubernetes.io/name: sts-delegate-rs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sts-delegate-rs
  ports:
    - name: http
      port: 8888
      targetPort: http
